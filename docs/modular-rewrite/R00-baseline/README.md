# R00 基线冻结与可重复验收：执行记录索引

## 阶段范围

冻结模块化重写开始前的远端提交、分支、保护资产、公共命令、数据库、前后端与 Windows 验收基线。R00 不实施业务重写，不移动源码，不修改模型保护区。

## 当前基线

- 基线分支：`main`
- 起始基线提交：`db79995873460688c15abb3497bf1c61b73ffb18`
- 实施分支：`new-A`
- 已完成节点：`R0-01 工作区状态与基线提交确认`

## 保护资产

R0-01 未修改模型、迁移、锁文件、契约、Schema、UI 基线或示例资产。保护资产指纹将在 R0-02 建立。

## 任务状态表

| 任务 ID | 任务名称 | 状态 | 实施记录 | 最小验证 | 阶段回归 |
|---|---|---|---|---|---|
| R0-01 | 工作区状态与基线提交确认 | DONE | [实施记录](./R00-01-工作区状态与基线提交确认.md) | 通过远端基线、分支存在性与分支起点核对；本地工作树检查受环境阻塞并已记录 | 未到阶段出口 |
| R0-02 | 模型保护资产指纹 | READY | 未创建 | 未执行 | 未执行 |
| R0-03 | 命令契约冻结 | BLOCKED | 未创建 | 未执行 | 未执行 |
| R0-04 | 数据库基线 | BLOCKED | 未创建 | 未执行 | 未执行 |
| R0-05 | 前端与 Rust 基线 | BLOCKED | 未创建 | 未执行 | 未执行 |
| R0-06 | Windows 基线 | BLOCKED | 未创建 | 未执行 | 未执行 |

## 实际文件变化累计

- 新增 `docs/modular-rewrite/R00-baseline/README.md`。
- 新增 `docs/modular-rewrite/R00-baseline/R00-01-工作区状态与基线提交确认.md`。
- 更新根 `README.md` 的模块化重写摘要。
- 未修改源码、配置、依赖、接口、数据、模型或运行行为。

## 已确认接口与兼容性变化

无。

## 未解决问题

- 当前执行环境无法建立本地 Git 工作树，因此不能读取用户设备上的未提交或未跟踪文件；本次仅在远端新分支写入，不会覆盖用户本地内容。

## 阶段门禁状态

未通过。R0-01 已完成；R0-02 至 R0-06 尚未完成，未创建 `R00-stage-completion.md`。

## 下一 READY 任务

唯一允许进入实施的下一节点为 `R0-02 模型保护资产指纹`。
