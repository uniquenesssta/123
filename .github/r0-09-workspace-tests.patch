diff --git a/crates/application/src/openai_research.rs b/crates/application/src/openai_research.rs
index 85819ec..0000000 100644
--- a/crates/application/src/openai_research.rs
+++ b/crates/application/src/openai_research.rs
@@ -507,7 +507,9 @@ mod tests {
         let schema = built_in_research_schema();
         assert_eq!(schema.schema_body["additionalProperties"], false);
         assert!(built_in_research_prompt()
             .content
+            .to_ascii_lowercase()
             .contains("do not calculate probabilities"));
     }
 }
diff --git a/crates/spreadsheet-io/src/team_package.rs b/crates/spreadsheet-io/src/team_package.rs
index 257391b..0000000 100644
--- a/crates/spreadsheet-io/src/team_package.rs
+++ b/crates/spreadsheet-io/src/team_package.rs
@@ -2038,6 +2038,7 @@ mod tests {
         let mut workbook = Workbook::new();
         let sheet = workbook.add_worksheet();
         sheet.set_name("球员与评分").expect("set player sheet name");
+        write_group_row(sheet, &[(0, 1, "操作")]).expect("write player group row");
         write_key_row(sheet, PLAYER_KEYS).expect("write player keys");
 
         let zero_based_physical_row = 7_u32;
