name: R5 Stage Closeout Docs

on:
  push:
    branches:
      - rewrite/r5-competition-routing-persistence
    paths:
      - .github/workflows/r5-stage-closeout-docs.yml

permissions:
  contents: write

jobs:
  closeout:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout stage branch
        uses: actions/checkout@v4
        with:
          ref: rewrite/r5-competition-routing-persistence
          fetch-depth: 0

      - name: Finalize R5 records and initialize R6 index
        shell: bash
        run: |
          python - <<'PY'
          from pathlib import Path
          import subprocess

          R5_START = "615dc952491d5e0e21d4979292cbf5170eedece6"
          R5_MERGE = "acb0491003b365b3f775780d8c98ecfdf1e80104"
          MERGED_RUN = "31884882480"
          MERGED_JOB = "95012628426"
          MERGED_ARTIFACT = "9247274708"
          MERGED_ARTIFACT_SIZE = "13936932"
          MERGED_DIGEST = "4c6b107922787a732537a83b58b005bb364ff10298146c87f0dff40f8bdf5a26"
          FIXED_HEAD = "058884e82f3c584f87751dec3bb5f9b6531a151e"
          PUSH_RUN = "31883239072"
          PUSH_JOB = "95008635990"
          PUSH_ARTIFACT = "9246859935"
          PUSH_DIGEST = "133baec479d64edd8217bb8d7c1c7f82b0171d2e6362c5d49277caf6b51413f1"
          PR_RUN = "31883703954"
          PR_JOB = "95009730520"
          PR_ARTIFACT = "9246957428"
          PR_DIGEST = "da854dc637f59e2832f4515b4154f46a4ae88a7fb7e141ce8ee18b36c132e001"

          root = Path("README.md")
          root_text = root.read_text(encoding="utf-8")
          old_root_status = "- 当前节点状态为 `VERIFYING`；详细记录见 `docs/modular-rewrite/R05-competition-routing-persistence/R05-06-model-run-identity-reads.md`，待文档 clean HEAD、PR/merge 与 merged-stage canonical gate 通过后关闭为 `DONE`。"
          if old_root_status not in root_text and "### R5 阶段完成" not in root_text:
              raise SystemExit("root README R5-06 status marker not found")
          if "### R5 阶段完成" not in root_text:
              new_root_status = f"""- R5-06 已正式关闭为 `DONE`；fixed clean HEAD `{FIXED_HEAD}` 的 push canonical run `{PUSH_RUN}` / job `{PUSH_JOB}` 与 PR #31 canonical run `{PR_RUN}` / job `{PR_JOB}` 均为 `SUCCESS`。
- PR #31 已按 fixed head squash merge 为 `{R5_MERGE}`；merged-stage canonical Public Platform CI run `{MERGED_RUN}` / Windows job `{MERGED_JOB}` 为 `SUCCESS`，artifact `{MERGED_ARTIFACT}`，SHA-256 `{MERGED_DIGEST}`。
- 详细节点记录：`docs/modular-rewrite/R05-competition-routing-persistence/R05-06-model-run-identity-reads.md`。

### R5 阶段完成

- R5-01～R5-06 均已完成并通过各自专项契约、阶段回归、clean PR 与 merged-stage canonical gate；Competition / Routing PostgreSQL persistence 已按职责收敛到 `crates/persistence-postgres/src/adapters/competition/` 与 `adapters/rules/`。
- 旧 `crates/persistence-postgres/src/competitions.rs` 与 `crates/persistence-postgres/src/routing.rs` 已删除，不保留双实现或长期转发壳；公共 Application Port、Tauri 命令/DTO、Schema、0001–0046 migration、配置、错误语义、路由/模型算法与生产依赖保持兼容。
- R5 阶段完成记录：`docs/modular-rewrite/R05-competition-routing-persistence/R05-stage-completion.md`。R6-01 `Team Directory 与 Detail` 为下一阶段唯一 `READY` 节点；R6 源码实施必须以本次 closeout tree 的 canonical CI 成功为进入基线。"""
              root_text = root_text.replace(old_root_status, new_root_status, 1)
              root.write_text(root_text, encoding="utf-8", newline="\n")

          stage = Path("docs/modular-rewrite/R05-competition-routing-persistence/README.md")
          stage_text = stage.read_text(encoding="utf-8")
          if "## 阶段状态\n\n`IN_PROGRESS`" in stage_text:
              stage_text = stage_text.replace("## 阶段状态\n\n`IN_PROGRESS`", "## 阶段状态\n\n`DONE`", 1)
          if "| R5-06 | Model Run Identity Reads | VERIFYING |" in stage_text:
              stage_text = stage_text.replace("| R5-06 | Model Run Identity Reads | VERIFYING |", "| R5-06 | Model Run Identity Reads | DONE |", 1)
          old_stage_baseline = "- R5 stage 分支 `rewrite/r5-competition-routing-persistence` 当前已完成 R5-02 closeout；R5-03 从最终验证基线 `bcd17c78dde89c999d7666fa41e7267f609c6a33` 精确建立。"
          if old_stage_baseline in stage_text:
              stage_text = stage_text.replace(old_stage_baseline, f"- R5 stage 分支 `rewrite/r5-competition-routing-persistence` 已完成 R5-01～R5-06；最终 R5-06 squash merge commit 为 `{R5_MERGE}`。", 1)
          old_current = "- 当前仅剩文档 clean HEAD、PR/merge、merged-stage canonical CI 与 R5 阶段 closeout，因此状态保持 `VERIFYING`。"
          if old_current in stage_text:
              stage_text = stage_text.replace(old_current, f"- fixed clean HEAD `{FIXED_HEAD}` 的 push canonical run `{PUSH_RUN}` / job `{PUSH_JOB}` 与 PR #31 canonical run `{PR_RUN}` / job `{PR_JOB}` 均 `SUCCESS`；PR #31 squash merge `{R5_MERGE}` 后 merged-stage canonical run `{MERGED_RUN}` / job `{MERGED_JOB}` 亦 `SUCCESS`。R5-06 正式关闭为 `DONE`。", 1)
          sentinel = "## R5 阶段正式收口"
          if sentinel not in stage_text:
              stage_text += f"""

{sentinel}

- R5-01～R5-06 全部 `DONE`；阶段完成记录：[`R05-stage-completion.md`](R05-stage-completion.md)。
- R5-06 fixed clean HEAD：`{FIXED_HEAD}`；push canonical run `{PUSH_RUN}` / job `{PUSH_JOB}`：`SUCCESS`；artifact `{PUSH_ARTIFACT}`，SHA-256 `{PUSH_DIGEST}`。
- PR #31 canonical run `{PR_RUN}` / job `{PR_JOB}`：`SUCCESS`；artifact `{PR_ARTIFACT}`，SHA-256 `{PR_DIGEST}`。
- PR #31 使用 expected head `{FIXED_HEAD}` squash merge为 `{R5_MERGE}`。
- merged-stage canonical run `{MERGED_RUN}` / Windows job `{MERGED_JOB}`：`SUCCESS`；artifact `{MERGED_ARTIFACT}`，大小 `{MERGED_ARTIFACT_SIZE}` 字节，SHA-256 `{MERGED_DIGEST}`。
- 旧 `competitions.rs` / `routing.rs` 均已删除；R5 目标 Ports 的 PostgreSQL owner 已收敛到新 Adapter，未保留双实现或临时转发层。
- 下一阶段唯一候选入口为 R6-01；R6 源码实施须等待本阶段 closeout 文档 tree 的 canonical Public Platform CI 成功后开始。
"""
          stage.write_text(stage_text, encoding="utf-8", newline="\n")

          node = Path("docs/modular-rewrite/R05-competition-routing-persistence/R05-06-model-run-identity-reads.md")
          node_text = node.read_text(encoding="utf-8")
          node_text = node_text.replace("状态：`VERIFYING`", "状态：`DONE`", 1)
          node_text = node_text.replace(
              "当前源码、专项 PostgreSQL 16 契约与 canonical Windows 全链验证均已通过；节点仍保持 `VERIFYING`，等待文档同步、clean PR、stage merge 与 merged-stage canonical CI 完成后再关闭为 `DONE`。",
              "当前源码、专项 PostgreSQL 16 契约、fixed clean HEAD canonical、clean PR canonical、squash merge 与 merged-stage canonical CI 均已完成并通过；R5-06 正式关闭为 `DONE`。",
              1,
          )
          old_pending = """源码/专项契约/canonical clean-source CI 已完成。当前未完成项仅为发布流程：

- R5-06 文档同步后的 fixed clean HEAD canonical CI；
- clean PR CI；
- squash merge 到 `rewrite/r5-competition-routing-persistence`；
- merged-stage canonical CI；
- R5 阶段完成记录与出口门禁。

因此本节点当前只能保持 `VERIFYING`。"""
          if old_pending in node_text:
              node_text = node_text.replace(old_pending, f"""源码、专项契约与节点发布流程均已完成：fixed clean HEAD `{FIXED_HEAD}` 的 push/PR canonical 均通过，PR #31 已 squash merge 为 `{R5_MERGE}`，merged-stage canonical run `{MERGED_RUN}` / job `{MERGED_JOB}` 通过。节点本身不存在未完成发布项。

R5 阶段仍需在独立 `R05-stage-completion.md` 中记录阶段级文件总表、限制、回退和出口门禁；该阶段 closeout 文档不会改变 R5-06 生产源码。""", 1)
          node_text = node_text.replace("- 根 README / 阶段 README：待本轮同步。", "- 根 README / 阶段 README：已同步。", 1)
          node_text = node_text.replace("- PR / merged-stage gate：待执行。", "- PR / merged-stage gate：已执行并通过。", 1)
          node_text = node_text.replace("**当前状态：`VERIFYING`，不得标记为 `DONE`。**", "**当前状态：`DONE`。**", 1)
          if "## 17. 正式收口证据" not in node_text:
              node_text += f"""

## 17. 正式收口证据

- fixed clean HEAD：`{FIXED_HEAD}`。
- 文档同步后 push canonical：run `{PUSH_RUN}` / job `{PUSH_JOB}` — `SUCCESS`；artifact `{PUSH_ARTIFACT}`，SHA-256 `{PUSH_DIGEST}`。
- PR #31 canonical：run `{PR_RUN}` / job `{PR_JOB}` — `SUCCESS`；artifact `{PR_ARTIFACT}`，SHA-256 `{PR_DIGEST}`。
- PR #31 使用 expected head `{FIXED_HEAD}` squash merge；stage merge commit：`{R5_MERGE}`。
- merged-stage canonical：run `{MERGED_RUN}` / Windows job `{MERGED_JOB}` — `SUCCESS`；artifact `{MERGED_ARTIFACT}`，SHA-256 `{MERGED_DIGEST}`。
- R5-06 自此正式 `DONE`；后续仅进入 R5 阶段级完成记录与 R6 基线建立，不再修改本节点生产实现。
"""
          node.write_text(node_text, encoding="utf-8", newline="\n")

          r6_dir = Path("docs/modular-rewrite/R06-entity-catalog-persistence")
          r6_dir.mkdir(parents=True, exist_ok=True)
          r6_readme = r6_dir / "README.md"
          if not r6_readme.exists():
              r6_readme.write_text(f"""# R06 Entity Catalog Persistence：执行记录索引

## 阶段状态

`IN_PROGRESS`

R6 按 Teams、Players、Coaches、Formations、Abilities、Dynamic Tags、Availability、Entity Matching、References、Deletion 与 Global Search 拆分 PostgreSQL Entity Catalog Adapter；不实施阵容提交、工作簿导入事务或 UI。

## 前置基线

- R5 阶段 R5-01～R5-06 已完成；阶段记录：[`../R05-competition-routing-persistence/R05-stage-completion.md`](../R05-competition-routing-persistence/R05-stage-completion.md)。
- R5 最终代码 merge commit：`{R5_MERGE}`；merged-stage canonical run `{MERGED_RUN}` / Windows job `{MERGED_JOB}` 为 `SUCCESS`。
- 本 README 与 R5 closeout 文档位于同一 closeout 提交；该提交的 canonical Public Platform CI 成功后，才作为 R6-01 源码实施基线。
- Team / Player Ports 与 Domain 公共对象保持冻结；0001–0046 migration 与模型保护资产继续冻结。

## 任务状态

| 任务 | 范围 | 状态 |
|---|---|---|
| R6-01 | Team Directory 与 Detail | READY |
| R6-02 | Team Names 与 Profiles | BLOCKED |
| R6-03 | Player Directory 与 Detail | BLOCKED |
| R6-04 | Player Names 与 Positions | BLOCKED |
| R6-05 | Team Periods 与 Availability | BLOCKED |
| R6-06 | Abilities 与 Dynamic Tags | BLOCKED |
| R6-07 | Coaches 与 Formation Usage | BLOCKED |
| R6-08 | Entity Matching 与 References | BLOCKED |
| R6-09 | Archive / Delete / Force Delete | BLOCKED |
| R6-10 | Global Name Search | BLOCKED |

## 当前边界

- R6 尚未修改任何生产源码、Schema、migration、配置、依赖或用户数据。
- R6-01 的目标目录为 `crates/persistence-postgres/src/adapters/catalog/teams/directory/` 与 `detail/`；在 closeout canonical baseline 成功前不得开始源码实施。
- 每个节点完成时必须创建对应 `R06-xx` 实施记录并更新本索引；R6 完成时必须创建 `R06-stage-completion.md`。
""", encoding="utf-8", newline="\n")

          # Remove this transient workflow before calculating the final stage tree.
          Path(".github/workflows/r5-stage-closeout-docs.yml").unlink(missing_ok=True)

          # Stage all prospective closeout changes and create a placeholder completion file
          # so the generated inventory describes the exact tree that will be committed.
          completion = Path("docs/modular-rewrite/R05-competition-routing-persistence/R05-stage-completion.md")
          completion.write_text("# R05 阶段完成记录\n", encoding="utf-8", newline="\n")
          subprocess.run(["git", "add", "-A"], check=True)

          diff = subprocess.check_output(
              ["git", "diff", "--cached", "--name-status", R5_START],
              text=True,
              encoding="utf-8",
          ).splitlines()

          added, modified, deleted, moved = [], [], [], []
          for line in diff:
              parts = line.split("\t")
              code = parts[0]
              if code == "A":
                  added.append(parts[1])
              elif code == "M":
                  modified.append(parts[1])
              elif code == "D":
                  deleted.append(parts[1])
              elif code.startswith("R") or code.startswith("C"):
                  moved.append((parts[1], parts[2], code))
              else:
                  raise SystemExit(f"unexpected git diff status: {line}")

          def path_table(paths):
              if not paths:
                  return "无。"
              return "| 文件 |\n|---|\n" + "\n".join(f"| `{p}` |" for p in paths)

          def moved_table(rows):
              if not rows:
                  return "无。"
              return "| 原文件 | 新文件 | Git 状态 |\n|---|---|---|\n" + "\n".join(
                  f"| `{old}` | `{new}` | `{code}` |" for old, new, code in rows
              )

          completion_text = f"""# R05 阶段完成记录

- 阶段状态：`DONE`
- R5 起点：`{R5_START}`
- R5 最终代码 merge commit：`{R5_MERGE}`
- 目标平台：Windows

## 1. 阶段目标与完成结论

R5-01 至 R5-06 均已 `DONE`。Competition、Season、Stage、Round、Rule Package、Competition Binding、Route Resolution 与 Model Run Identity PostgreSQL persistence 已从职责混合的旧模块收敛到 `crates/persistence-postgres/src/adapters/competition/` 与 `adapters/rules/` 的具名目录；查询、写入、typed Row、Domain Mapper 与多表 transaction 按职责拆分。

旧 `crates/persistence-postgres/src/competitions.rs` 与 `crates/persistence-postgres/src/routing.rs` 已删除，不保留双实现或长期转发壳。R5 不修改路由决策算法、模型算法、公共 Application Port、Tauri 命令/DTO、数据库 Schema、0001–0046 migration、配置、错误语义或生产依赖。

R5-06 PR #31 已使用 fixed clean HEAD `{FIXED_HEAD}` squash merge为 `{R5_MERGE}`；merged-stage canonical Public Platform CI run `{MERGED_RUN}` / Windows job `{MERGED_JOB}` 为 `SUCCESS`，因此 R5 阶段出口的源码与发布门禁均已满足。

## 2. 已完成节点索引

| 任务 ID | 实施记录 | 完成状态 | 最小/关键验证 |
|---|---|---|---|
| R5-01 | [`R05-01-competitions-repository.md`](R05-01-competitions-repository.md) | DONE | old-owner PG contract `31772436658`; Windows frontend `31773777077`; workspace Rust `31773900831`; PR `31775501711`; merged stage `31777130456` |
| R5-02 | [`R05-02-seasons-stages-and-rounds.md`](R05-02-seasons-stages-and-rounds.md) | DONE | hard gate `31794402789`; PR `31794818136`; merged stage `31796688330` |
| R5-03 | [`R05-03-rule-packages.md`](R05-03-rule-packages.md) | DONE | old/new PG contracts `31810196287` / `31811073556`; hard gate `31811535324`; PR `31812316772`; merged stage `31824523536` |
| R5-04 | [`R05-04-competition-bindings.md`](R05-04-competition-bindings.md) | DONE | old/new PG contracts `31862339200` / `31862713824`; hard gate `31862969585`; PR `31863321636`; merged stage `31864520861` |
| R5-05 | [`R05-05-route-resolution-reads.md`](R05-05-route-resolution-reads.md) | DONE | old/new PG contracts `31869022228` / `31869137395`; hard gate `31869727870`; PR `31873912016`; merged stage `31875035071` |
| R5-06 | [`R05-06-model-run-identity-reads.md`](R05-06-model-run-identity-reads.md) | DONE | isolated PG `31881761988`; source canonical `31881842368`; fixed-head push `{PUSH_RUN}`; PR `{PR_RUN}`; merged stage `{MERGED_RUN}` |

## 3. 实际新增文件总表

共 {len(added)} 个新增文件。

{path_table(added)}

## 4. 实际修改文件总表

共 {len(modified)} 个修改文件。

{path_table(modified)}

## 5. 实际移动或重命名文件总表

{moved_table(moved)}

## 6. 实际删除文件总表

共 {len(deleted)} 个删除文件。

{path_table(deleted)}

## 7. 最终目录与职责边界

- `adapters/competition/directory/`：Competition create/list/delete；soft delete 的锁定、binding 停用、external ID 清理与 competition soft-delete 各自独立，transaction 只编排。
- `adapters/competition/detail/`：Competition detail SELECT、typed Row 与 Domain Mapper。
- `adapters/competition/hierarchy/seasons/`、`stages/`、`rounds/`：各层级独立 create/read/list、typed Row 与 mapper。
- `adapters/rules/packages/`：Rule Package register/list、source document upsert、typed Row/mapper 与 registration transaction。
- `adapters/competition/bindings/`：Binding list/detail/create/type-default、package route metadata、typed Row/mapper 与具名 transaction。
- `adapters/competition/route_resolution/context/`：Competition hierarchy context read 与 scope validation；`route/`：explicit package、binding candidate、result row/mapper 与 route read coordinator。
- `adapters/competition/model_run_identity/`：单次 Model Run identity read、typed Row/record mapper；`registration/`：definition/version/parameter-set 与 model registration transaction。
- `model_runs.rs` 只编排 run document 本体并委托 identity owner；不再直接拥有 identity JOIN。
- `competitions.rs` / `routing.rs` 已从最终树删除。

## 8. 最终调用流、数据流和状态所有权

Competition / Rules Application Port -> Application composition adapter -> `PostgresStore` -> R5 具名 adapter query/command -> typed SQLx Row -> Domain Mapper -> Domain / route result。

Model Run detail 的 identity 路径为 `model_runs::read_run -> read_model_run_identity -> typed Row -> identity record`；Rule Package registration 通过 crate 内 `register_model_in_tx` 进入 `model_run_identity/registration/` 唯一事务边界。

数据库继续是 Competition、hierarchy、binding 与 model identity 的事实源；R5 未新增进程内业务缓存、Repository 全局工作流状态或第二状态 owner。

## 9. 公共接口、DTO、Schema、数据与配置变化

- Application Competition / Rules Ports：名称、参数、返回类型和调用语义未改变。
- Tauri 公共命令、DTO 与前端调用契约：未改变。
- PostgreSQL Schema、0001–0046 migration 与持久化格式：未改变。
- Competition/Season/Stage/Round ID、Binding 关系与历史运行引用：未改变。
- 配置键、默认值、环境变量、日志等级和生产依赖：未改变。
- `ModelRegistration` 公共导出、`PostgresStore::register_model` 调用语义及 `read_run` identity JSON 字段：未改变。
- 路由决策算法与模型算法：未改变。

## 10. 保持不变的兼容行为

- Competition CRUD、层级读取与现有 binding 语义保持。
- Route specificity/fallback、有效期、模型过滤、explicit package、reason/error 语义保持。
- Model identity 的 model/version/parameter/rule-package/binding nullable 语义保持。
- 模型版本/参数版本冲突错误保持既有语义。
- 历史 migration 内容冻结，R5 未执行任何 migration 改写。
- `crates/model-api/`、`crates/model-p4/`、`crates/model-p7/` 与关联受保护资产未作为 R5 修改范围。

## 11. 旧实现、重复实现和临时路径清理结果

- `crates/persistence-postgres/src/competitions.rs`：删除。
- `crates/persistence-postgres/src/routing.rs`：删除。
- R5-01～R5-06 的 transient PostgreSQL / inventory / diagnostic workflow 均在对应验证后清理，最终 stage tree 不保留临时 gate。
- 未保留 `old/new/legacy/copy/final/v2` 长期业务目录或无退出计划兼容层。
- 所有生产入口在各节点内完成唯一 owner 切换；失败验证 tree 未合并到 stage 分支。

## 12. 阶段级验证与真实结果

- R5-01：Competition Repository PostgreSQL 16 contract `31772436658` / job `94680947724` `SUCCESS`；Windows frontend `31773777077` / job `94684879652` `SUCCESS`；workspace Rust `31773900831` / job `94685243308` `SUCCESS`；PR #26 `31775501711` 与 merged-stage `31777130456` 均 `SUCCESS`。
- R5-02：最终 hard gate `31794402789` / job `94748190282` `SUCCESS`；PR `31794818136` / job `94749470982` 与 merged-stage `31796688330` / job `94755204665` `SUCCESS`。
- R5-03：old/new Rule Package PG contracts `31810196287` / `31811073556` 均 `SUCCESS`；hard gate `31811535324`、PR `31812316772`、merged-stage `31824523536` 均 `SUCCESS`。
- R5-04：old/new Binding PG contracts `31862339200` / `31862713824` 均 `SUCCESS`；hard gate `31862969585`、PR `31863321636`、merged-stage `31864520861` 均 `SUCCESS`。
- R5-05：old/new Route Resolution PG contracts `31869022228` / `31869137395` 均 `SUCCESS`；hard gate `31869727870`、PR #30 `31873912016`、merged-stage `31875035071` 均 `SUCCESS`。
- R5-06：isolated PostgreSQL 16 run `31881761988` 的 identity job `95005200186` 与 Rule Package regression job `95005200191` 均 `SUCCESS`；clean source canonical `31881842368` / job `95005434692` `SUCCESS`；fixed clean HEAD `{FIXED_HEAD}` 的 push canonical `{PUSH_RUN}` / `{PUSH_JOB}` 与 PR #31 canonical `{PR_RUN}` / `{PR_JOB}` 均 `SUCCESS`。
- PR #31 squash merge `{R5_MERGE}` 后 merged-stage canonical Public Platform CI `{MERGED_RUN}` / Windows job `{MERGED_JOB}`：`SUCCESS`；artifact `{MERGED_ARTIFACT}`，大小 `{MERGED_ARTIFACT_SIZE}` 字节，SHA-256 `{MERGED_DIGEST}`。
- canonical Windows gate 实际覆盖 architecture verifier、公开/受保护模型边界、frontend verification、Cargo.lock/rustfmt/workspace Clippy `-D warnings`、workspace tests、Tauri build、release binary startup 与 runtime error scan。

## 13. 未执行验证、环境阻塞和剩余风险

- R5 没有在用户现有 PostgreSQL 数据库执行写入、destructive reset 或 broad 18 项 ignored `postgres_integration.rs` 全量回归；各节点使用 GitHub Actions 临时 PostgreSQL 16 专用测试库执行 domain-specific contract。历史数据库兼容由冻结的 0001–0046 migration、既有 schema contract 与节点 PG contracts覆盖，但本阶段没有把“用户真实现有 DB sample”描述为已执行。
- R5-01 Ubuntu frontend diagnostic 曾因 Chromium `DevToolsActivePort` 环境阻塞，Ubuntu workspace Rust 曾因 `glib-2.0/gobject-2.0` 缺失阻塞；相同目标源码的 Windows frontend 与 Windows workspace Rust 已分别实际通过。
- R5-05 Ubuntu 专项 gate同样受 runner `glib-2.0` 系统库缺失阻塞；相同源码的 canonical Windows gate实际通过。
- Windows `Full` 人工交互验收与用户本机数据库验收未由云端 Automated 替代；本阶段没有将其虚报为通过。
- 未发现需要新增生产依赖、Schema migration 或兼容层的剩余风险。

## 14. 根 README、阶段 README 与架构文档同步

- 根 `README.md` 已记录 R5-06 与 R5 阶段实际实现、兼容边界、关键验证和完成记录链接。
- 本阶段 `README.md` 已将 R5-01～R5-06 全部标记为 `DONE`，并记录 PR #31、merged-stage canonical 与阶段完成记录。
- `architecture/domain-type-inventory.json` 仅使用项目官方 generator 在节点变更后刷新；没有手工放宽 drift gate。
- 本阶段未改变 `docs/ARCHITECTURE.md` 所述系统层级或公共架构契约，因此没有为无实际架构变化制造额外文档改写。

## 15. 阶段回退点与回退步骤

R5 起点为已验证 R4 closeout `{R5_START}`。R5 最终代码 merge commit 为 `{R5_MERGE}`。

如需整体回退 R5，应通过 Git 回退到 `{R5_START}` 或对 R5 的阶段提交执行受控 revert，并重新运行数据库/路由/模型 identity 契约与 canonical Windows gate；不得通过复制已删除的 `competitions.rs` / `routing.rs` 恢复双实现。

## 16. 出口门禁逐项结论

- `docs/modular-rewrite/R05-competition-routing-persistence/README.md` 完整索引 R5-01～R5-06：`PASS`。
- `R05-stage-completion.md` 已创建并包含真实文件总表、验证、限制、回退与订正：`PASS`。
- 赛事与路由持久化旧模块 `competitions.rs` / `routing.rs` 删除：`PASS`。
- Competition / Rules 相关 PostgreSQL Ports 由新 Adapter 唯一实现且无双 owner：`PASS`。
- 路由结果与 Model Run identity 契约保持：`PASS`。
- 模型保护资产、Schema/migration、生产依赖无未批准变化：`PASS`。
- R6 可进入初始化/唯一 `READY` 节点：`PASS`；R6-01 为下一入口。

## 17. 下一阶段唯一 READY 任务

`R6-01 — Team Directory 与 Detail`。

目标目录：

```text
crates/persistence-postgres/src/adapters/catalog/teams/directory/
crates/persistence-postgres/src/adapters/catalog/teams/detail/
```

R6 阶段索引已初始化于 `docs/modular-rewrite/R06-entity-catalog-persistence/README.md`。R6-01 不得在本次 closeout 文档 tree 的 canonical Public Platform CI 成功前开始生产源码实施。

## 18. 订正记录

- R5-06 名称原为 Model Run Identity Reads，但 R5-05 后 `routing.rs` 唯一残余职责还包含模型注册 identity 写入。为满足“唯一 Adapter owner + 删除旧 routing module”的阶段出口，R5-06 在同一 identity 边界内同步迁移 `ModelRegistration` 与 registration transaction；未扩展到模型/路由算法。
- R5-06 首轮 PostgreSQL workflow 让两个 ignored contract 复用同一数据库，第二项因重复 migration 出现 `relation \"settings\" already exists`；随后改为隔离 PostgreSQL 16 service database，最终双 job 实际通过。该失败保留在节点记录中，没有弱化或跳过测试。
- R5-05 PR 首轮 canonical Clippy 因未使用 typed `RouteRow.competition_kind` 失败；只删除未消费 Row 字段与冗余 SELECT 返回列，保留 WHERE 过滤与 route 语义，失败 tree 未合并。
- 本阶段所有环境阻塞与失败门禁均保留事实记录；没有把未执行、被阻塞或失败的验证描述为通过。
"""

          completion.write_text(completion_text, encoding="utf-8", newline="\n")
          subprocess.run(["git", "add", "-A"], check=True)

          # Fail closed if the transient workflow somehow remains in the final tree.
          staged = subprocess.check_output(["git", "diff", "--cached", "--name-status", R5_START], text=True)
          if ".github/workflows/r5-stage-closeout-docs.yml" in staged:
              raise SystemExit("transient closeout workflow leaked into final stage tree")
          print(f"R5 closeout inventory: added={len(added)} modified={len(modified)} moved={len(moved)} deleted={len(deleted)}")
          PY

      - name: Commit closeout records
        shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          if git diff --cached --quiet; then
            echo "No closeout changes to commit."
            exit 0
          fi
          git commit -m "docs(r5): close stage and open r6"
          git push origin HEAD:rewrite/r5-competition-routing-persistence
