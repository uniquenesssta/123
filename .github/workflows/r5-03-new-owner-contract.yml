name: R5-03 Rule Package New Owner Contract

on:
  push:
    branches:
      - agent/r5-03-rule-packages
    paths:
      - ".github/workflows/r5-03-new-owner-contract.yml"

permissions:
  contents: read

jobs:
  rule-package-contract:
    runs-on: ubuntu-24.04
    timeout-minutes: 30
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: football_test
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U postgres -d football_test"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    env:
      FOOTBALL_TEST_DATABASE_URL: postgres://postgres:postgres@127.0.0.1:5432/football_test
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@1.88.0
      - name: Verify new-owner architecture
        run: node scripts/verify-rule-package-repository.mjs
      - name: Run identical Rule Package contract on new owner
        run: cargo test --locked -p football-persistence-postgres --test rule_package_repository_contract -- --ignored --test-threads=1
