name: R3-06 Prediction Core Migration

on:
  push:
    branches:
      - rewrite/r3-06-prediction-service

permissions:
  contents: write

jobs:
  migrate:
    if: github.actor != 'github-actions[bot]'
    runs-on: windows-2025
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@v4
        with:
          ref: rewrite/r3-06-prediction-service
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - uses: dtolnay/rust-toolchain@1.88.0
        with:
          components: rustfmt
      - name: Generate phase 1 modules
        shell: pwsh
        run: |
          python scripts/r3-06-phase1.py
          python scripts/r3-06-phase1-fix.py
      - name: Format and refresh inventory
        shell: pwsh
        run: |
          cargo fmt --all
          node scripts/generate-domain-type-inventory.mjs
      - name: Validate phase 1
        shell: pwsh
        run: |
          node scripts/verify-application-ports.mjs
          npm run verify:architecture
          cargo fmt --all -- --check
          cargo check --locked -p football-application
          cargo test --locked -p football-application
      - name: Commit clean phase 1
        shell: pwsh
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git rm .github/workflows/r3-06-scan.yml
          git rm .github/workflows/r3-06-phase1.yml
          git rm scripts/r3-06-phase1.py
          git rm scripts/r3-06-phase1-fix.py
          git add -A
          git diff --check
          git commit -m "refactor(r3): migrate prediction core service"
          git push origin HEAD:rewrite/r3-06-prediction-service
